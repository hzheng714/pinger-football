import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pinger.football',
  appName: 'pinger-football',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
